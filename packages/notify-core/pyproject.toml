[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "notify-core"
version = "0.1.0"
description = "Cross-platform desktop notification library with event bus"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
